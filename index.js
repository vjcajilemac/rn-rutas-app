/**
 * @format
 */

import { AppRegistry } from 'react-native';
import RutasApp from './src/RutasApp'
import { name as appName } from './app.json';

AppRegistry.registerComponent(appName, () => RutasApp);
